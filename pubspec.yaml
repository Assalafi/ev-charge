name: ev_charge
description: EV Charge - Smart EV Charging App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.5
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  google_fonts: ^4.0.4
  mobile_scanner: ^3.5.6
  shimmer: ^3.0.0
  intl: ^0.17.0
  percent_indicator: ^4.2.3
  cached_network_image: ^3.3.0
  flutter_paystack: ^1.0.7
  local_auth: ^2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
